# Configuração nginx para a VM de reverse proxy
# Domínio: w.casadosparafusosvta.com
#
# Pré-requisitos na VM de proxy:
#   1. nginx instalado
#   2. certbot instalado:  sudo apt install certbot python3-certbot-nginx
#   3. DNS: registro A de w.casadosparafusosvta.com → IP desta VM de proxy
#   4. Porta 80 e 443 abertas no firewall
#
# Passos para ativar:
#   sudo cp nginx-proxy-vm.conf /etc/nginx/sites-available/w.casadosparafusosvta.com
#   sudo certbot certonly --nginx -d w.casadosparafusosvta.com
#   sudo ln -s /etc/nginx/sites-available/w.casadosparafusosvta.com \
#              /etc/nginx/sites-enabled/
#   sudo nginx -t && sudo systemctl reload nginx
#
# Substitua <IP_DA_VM_BOTCDP> pelo IP da VM onde rodam os containers Docker.

upstream botcdp_frontend {
    server <IP_DA_VM_BOTCDP>:9001;
    keepalive 32;
}

server {
    listen 80;
    server_name w.casadosparafusosvta.com;
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl http2;
    server_name w.casadosparafusosvta.com;

    ssl_certificate     /etc/letsencrypt/live/w.casadosparafusosvta.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/w.casadosparafusosvta.com/privkey.pem;
    include             /etc/letsencrypt/options-ssl-nginx.conf;
    ssl_dhparam         /etc/letsencrypt/ssl-dhparams.pem;

    client_max_body_size 15M;

    location / {
        proxy_pass         http://botcdp_frontend;
        proxy_http_version 1.1;

        # WebSocket — necessário para Socket.IO
        proxy_set_header Upgrade    $http_upgrade;
        proxy_set_header Connection "upgrade";

        proxy_set_header Host              $host;
        proxy_set_header X-Real-IP         $remote_addr;
        proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # Manter conexões WebSocket abertas (Socket.IO de longa duração)
        proxy_read_timeout 3600s;
        proxy_send_timeout 3600s;
    }
}
